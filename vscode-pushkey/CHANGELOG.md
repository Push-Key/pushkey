# Changelog

## 0.1.0 — Initial release

- Gutter decorations for `.env` files based on `~/.pushkey/health.json`.
- Three states: healthy / warning / critical.
- `Pushkey: Refresh Key Health` command.
- File watcher auto-refreshes when `health.json` changes.
