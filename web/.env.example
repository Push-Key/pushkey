# URL of the pushkey_cloud_api backend (FastAPI server)
NEXT_PUBLIC_ADMIN_API_URL=http://localhost:8000

# Admin password for /admin/* routes (must match ADMIN_SECRET on the backend)
PUSHKEY_ADMIN_SECRET=dev-change-me
