[build]
builder = "nixpacks"
buildCommand = "pip install -r requirements-api.txt"

[deploy]
startCommand = "uvicorn pushkey_cloud_api:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
