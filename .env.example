# Pushkey Cloud API — environment variables
# Copy to .env and fill in real values, or set as platform secrets.

# ── Required for production ─────────────────────────────────────
PUSHKEY_ADMIN_EMAIL="admin@example.com"
PUSHKEY_ADMIN_PASSWORD="GenerateWithPythonSecrets"
PUSHKEY_JWT_SECRET="GenerateAnother64HexCharsHere"
PUSHKEY_ADMIN_SESSION_TTL_MIN="30"
PUSHKEY_ADMIN_COOKIE_SECURE="true"

# Optional bootstrap TOTP secret. Phase 3 will replace this bootstrap setting
# with per-administrator MFA enrollment and recovery codes.
PUSHKEY_ADMIN_TOTP_SECRET=""

# ── Storage ─────────────────────────────────────────────────────
PUSHKEY_DATA_DIR="/data"

# ── SMTP (for invite emails) ────────────────────────────────────
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="you@example.com"
SMTP_PASS="app-password-not-real-password"
FROM_EMAIL="you@example.com"

# ── Frontend ────────────────────────────────────────────────────
APP_URL="https://pushkey.app"
ADMIN_ORIGIN="https://admin.pushkey.app"

# ── Token TTL (hours) ───────────────────────────────────────────
PUSHKEY_TOKEN_TTL_HOURS="720"
